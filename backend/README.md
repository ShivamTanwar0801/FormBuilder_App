# Backend - Form Builder (Starter)

Install & run:
```
cd backend
npm install
cp .env.example .env   # update .env
npm run dev
```
This is an Express + Mongoose starter with simple routes for forms and responses.
